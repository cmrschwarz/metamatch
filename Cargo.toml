[package]
name = "metamatch"
version = "0.1.0"
edition = "2021"
license = "MIT"

# defined by let .. else statements
rust-version = "1.65.0"

[dependencies]

[dev-dependencies]
paste = "1.0.15"

[lib]
proc-macro = true
test = false

[package.metadata.docs.rs]
targets = ["x86_64-unknown-linux-gnu"]
